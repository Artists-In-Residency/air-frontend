import React, { Component } from 'react'

export default class ResidencyCard extends Component {
    render() {
        return (
            <li>
                Individual Residency Listing
                {/* <p><strong>{this.props.item.name}</strong> | {this.props.item.ticker} <button onClick={() => this.props.handleFavorite(this.props.item)}>MAKE FAVORITE</button></p> */}
            </li>
        )
    }
}